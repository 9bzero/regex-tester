# regex-tester

Write a regex, test it against text, see matches highlighted in real time.

## Features

- Flags: global, case-insensitive, multiline, dotAll
- All matches highlighted inline
- Match list with capture groups
- Pattern explanation (hover over each part)
- Common patterns library: email, URL, phone, date, etc.

## Run

```bash
npm install && npm run dev
```
