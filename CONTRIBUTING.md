# Contributing

Found a bug? Open an issue with the request that failed, the expected response, and what actually happened.

Adding a feature? Open an issue first. Dev tools can get complicated fast — better to align before writing code.

```bash
npm install
npm run dev
```

Please keep changes small. If a PR touches more than 3 files, it's probably two PRs.
